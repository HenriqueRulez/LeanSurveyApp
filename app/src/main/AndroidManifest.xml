<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coderulez.senai.leansurvey">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.OpcoesActivity" />
        <activity android:name=".activity.InicioActivity" />
        <activity android:name=".activity.AjudaActivity" />
        <activity android:name=".activity.QuestaoITActivity" />
        <activity android:name=".activity.QuestaoCBActivity" />
        <activity android:name=".activity.QuestionRBActivity" />
        <activity
            android:name=".activity.NavigationDrawer">


        </activity>
    </application>

</manifest>